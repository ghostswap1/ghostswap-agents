{
  "mcpServers": {
    "ghostswap": {
      "command": "npx",
      "args": ["-y", "@ghostswapio/mcp"],
      "env": {
        "GHOSTSWAP_PUBLIC_KEY": "${GHOSTSWAP_PUBLIC_KEY}",
        "GHOSTSWAP_SECRET": "${GHOSTSWAP_SECRET}"
      }
    }
  }
}
